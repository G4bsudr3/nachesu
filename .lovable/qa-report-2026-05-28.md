# QA Report — 2026-05-28 (parcial v2)

Rodada 2 concluiu o módulo 1 da IA com `frattz@naches.app` (PBL + reflexão final + módulo concluído + tutor IA). Etapas 3-5 (Economia Circular, conta com 2 matrículas, admin) ainda não rodaram. `qa-bootstrap` continua deployada.

## Status atualizado

- Etapa 1 (senhas): ✅
- Etapa 2 (IA, frattz@naches.app): ✅ módulo 1 completo, PBL entregue, reflexão final salva, módulo concluído, tutor IA conversado 1 turno (resposta veio com gradient roxo-rosa visualmente do tutor — ok).
- Etapas 3, 4, 5 (Eco, mateusfrattezi, admin): ⏸ não rodadas
- Etapa 6 limpeza + Etapa 7 plano final: ⏸

## Achados novos (rodada 2)

### T1 — quebra

**B1. Trigger `handle_new_user` NÃO criou profile nem user_roles para os usuários gerados via `admin.auth.admin.createUser` (qa-bootstrap).**
Tive que rodar INSERT manual em `profiles` e `user_roles` pros 2 users novos (`frattz@naches.app`, `mateus.frattz@edu.sebrae.com.br`). Sem profile, o tutor consent quebrou porque `UPDATE profiles SET tutor_consent_at` afetou 0 linhas (sem erro), e o modal entrou em loop reabrindo eternamente.
**Causa raiz suspeita:** trigger `on_auth_user_created` pode não estar ativado em prod, ou o `handle_new_user` falhou silenciosamente. Em `<db-triggers>` aparece "no triggers" — bandeira vermelha. Conferir se trigger existe e adicionar fallback no front (criar profile lazy se faltar).
**Esforço:** M.

**B2. Tutor consent loop infinito quando UPDATE não afeta linha.**
`TutorConsentModal.tsx:24-34` faz update e só checa `error`. Se 0 linhas afetadas, segue como sucesso → mas o parent reabre o modal porque consent_at continua null. Adicionar checagem `.select()` no update, ou retornar e exibir mensagem "perfil incompleto, recarrega".
**Esforço:** S.

### T2 — fricção média

**B3. Botão "ENTRAR" do `/auth` não revela formulário se modal "primeira vez" sobrepõe (já apontado A7, agora confirmado tampa tudo em mobile).**

**B4. Mensagem "ainda falta uma pílula · falta 0 pílulas obrigatórias" no card black quando estado tá entre carregamentos (skeleton). Render race condition.**
Arquivo: provavelmente `src/pages/eletiva/ModulePage.tsx`. Mostrar skeleton dedicado em vez de copy contraditória.
**Esforço:** S.

### T3 — polimento

**B5. Tutor IA: resposta veio em balão com gradient laranja→rosa→roxo (cara de "AI generic UI"). Workspace knowledge proíbe gradient roxo-rosa de AI generic.** Trocar pro estilo da paleta Perestroika (rosa puro `--primary` ou bege com borda).
**Esforço:** S.

**B6. Após módulo concluído, scrollei e a página não navegou de volta nem mostrou celebração — só ficou no botão "MÓDULO CONCLUÍDO" disabled.** Falta signature moment de conclusão (confetti sutil, Lottie do joão-de-barro celebrating, ou redirect com toast).
**Esforço:** M.

## Achados rodada 1 (mantidos)

A1-A10 do report anterior continuam válidos.

## Pendências

1. Etapa 3 — QA Eco Circular (`mateus.frattz@edu.sebrae.com.br`)
2. Etapa 4 — QA 2 matrículas + safety barrier (`mateusfrattezi@gmail.com`)
3. Etapa 5 — QA admin (`hey@frattz.com`)
4. Etapa 6 — apagar `qa-bootstrap`
5. Etapa 7 — plano final priorizado

Me chame "continua o QA etapa 3" pra eu seguir.
