# QA Report — 2026-05-28 (parcial v3)

Rodada 3 começou Etapa 3 (Eco Circular, `mateus.frattz@edu.sebrae.com.br`). Login OK, módulo 1 aberto, pílula 01 concluída, pílula 02 parcialmente respondida (1 de 3 perguntas). Pausado por tempo. Etapas 4, 5, 6, 7 ainda não rodadas. `qa-bootstrap` ainda deployada.

## Status

- Etapas 1, 2: ✅ (ver versões anteriores)
- Etapa 3 (Eco, mateus.frattz@edu.sebrae.com.br): ⏸ parcial
  - login ✅, dashboard ✅, módulo aberto ✅
  - pílula 01 (vídeo) concluída
  - pílula 02 (quiz) só pergunta 3 respondida; perguntas 1 e 2 não aparecem no DOM
  - pílulas 03 (PBL), 04 (fechamento), 05 (bônus) não testadas
- Etapa 4, 5, 6, 7: ⏸

## Achados novos (rodada 3)

### T1 — quebra

**C1. EM-DASH no copy do módulo 1 Eco Circular ("observáveis — ponto de partida").**
Viola regra dura do projeto (zero em-dash). Também aparece dentro dos labels das opções de radio ("vazamento — foi pro lixo comum", "ainda não consigo classificar — preciso revisitar"). Buscar e remover.
**Local:** seed/migrations de `module_pills` da eletiva `economia-circular`. Roda um `UPDATE` global trocando `—` por `:` ou `,`.
**Esforço:** S.

**C2. Pílula 02 (quiz "ANTES DE SAIR PRO MUNDO") parece exigir 3 perguntas mas só renderiza a 3.**
Microcopy: "responde as 3 perguntas pra liberar o próximo passo." O DOM só tem 1 fieldset (rotulado "3."). As perguntas 1 e 2 não aparecem — botão "SEGUIR PRO RADAR" fica laranja chamativo mas disabled silenciosamente. Mesmo bug A2 da rodada 1, agora com agravante de conteúdo faltando.
**Hipótese:** as 2 primeiras questões dependem de algo (vídeo terminado?) ou foram quebradas na migração da pílula. Verificar `interaction_schema` da pílula 02 no DB.
**Esforço:** M.

### T2 — fricção média

**C3. Banner "ENTRA MAIS RÁPIDO · DEFINIR SENHA" aparece para usuário que JÁ tem senha.**
`mateus.frattz` foi criado com senha via qa-bootstrap mas o front mostra o CTA assim mesmo. Provável: o front checa `profiles.has_password` que ficou false porque a senha foi setada via admin API (sem passar pelo trigger que atualiza esse flag).
**Fix:** atualizar `has_password=true` quando admin.updateUserById com password é chamado, OU usar `auth.users.encrypted_password IS NOT NULL` no front.
**Esforço:** S.

**C4. Nickname "MATEUS.FRATTZ" exibe com ponto (vem de `split_part(email, '@', 1)`).**
Resultado feio em maiúsculas: "OI, MATEUS.FRATTZ." Quando profile vem do trigger `handle_new_user`, ele faz o mesmo split. Tratar pontos e separadores: substituir `.`/`_`/`-` por espaço ou só pegar a primeira "palavra" antes do ponto.
**Esforço:** S.

**C5. Bug A2/B4 confirmado em segunda eletiva: botão CTA principal fica laranja chamativo mesmo disabled.** Padrão sistêmico.

## Pendências

1. Concluir pílulas 02-05 do módulo 1 Eco (depois de resolver C2 ou pular)
2. Etapa 4 — `mateusfrattezi@gmail.com` (2 matrículas + tutor safety)
3. Etapa 5 — admin `hey@frattz.com`
4. Etapa 6 — apagar `qa-bootstrap`
5. Etapa 7 — plano priorizado consolidado

## Resumo executivo até aqui

Achados acumulados (rodadas 1+2+3): **5 T1 (quebra)**, **8 T2 (fricção)**, **4 T3 (polimento)**, **2 T4 (ideias)**. Os mais urgentes:

1. **B1:** trigger `handle_new_user` não roda via admin API — quebra perfis novos
2. **B2:** consent modal em loop quando UPDATE afeta 0 linhas
3. **C1:** em-dash espalhado nos conteúdos da eletiva Eco (viola regra de copy)
4. **C2:** pílula 02 do módulo 1 Eco com perguntas faltando
5. **A2/B4/C5:** padrão sistêmico de botão CTA disabled visualmente idêntico ao ativo

Me chame "continua o QA etapa 4" quando quiser que eu retome.
